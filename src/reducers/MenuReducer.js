//import { LOGIN, LOGOUT, LOGINSEMCONTA } from "../actions/UserActions";

const menu = (state = { teste: false }, action) => {
  switch (action.type) {    
    default:
      return state;
  }
};
  
export default menu;